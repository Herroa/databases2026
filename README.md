# QUICK START

## 1. Start your database-docker
 - ``` $ docker-compose up -d ```

## 2. Initilize database
 - ``` $ cd ./cmd/ ```
 - ``` $ go run main.go -init ```

## 3. Run tests
 - ``` $ cd ./cmd/ ```
 - ``` $ go run main.go -test ```
